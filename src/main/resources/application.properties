# MongoDB connection settings can be overridden via environment variables in production.
spring.data.mongodb.uri=mongodb://localhost:27017/e_ticaret_sitesi

# Configure logging level if needed
logging.level.org.springframework=INFO
